Endpoint to delete registered users
---
parameters:
  - name: user_id
    in: path
    type: number
    required: true
    default: 1
responses:
  200:
    description: A boolean indicating if the user was deleted or not
    examples:
      wasUserDeleted: true

